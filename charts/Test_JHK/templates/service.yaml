apiVersion: v1
kind: Service
metadata:
  name: {{ template "Test_JHK.fullname" . }}
  labels:
    app: {{ template "Test_JHK.name" . }}
    chart: {{ template "Test_JHK.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "Test_JHK.name" . }}
    release: {{ .Release.Name }}
